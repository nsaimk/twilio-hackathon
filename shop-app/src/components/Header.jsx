import React from 'react';

const Header = () => {
    return (
        <header style={{ backgroundColor: '#f1f1f1', padding: '20px', textAlign: 'center', position: 'fixed', width: '100%', top: 0 }}>
            <h1>Customer App</h1>
        </header>
    );
};

export default Header;